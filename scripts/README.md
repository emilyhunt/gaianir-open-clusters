# Scripts to execute the project

When ran in order, the scripts in this directory will exactly reproduce the results of the project. These scripts may take some time to run or take some somewhat serious compute to do so - this is notated at the top of each file.